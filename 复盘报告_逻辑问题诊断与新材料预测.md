# 二维热电图谱复盘：逻辑问题诊断 + 易算特征假设 + 新材料预测

> 复盘对象：`jarvis_2d_te_atlas`（JARVIS dft_2d，1103 二维材料）及现有结论报告。
> 复盘方式：**逐条用数据复算**关键数字，再据此做逻辑审查；并新增"易算特征↔热电参数"关联假设与候选材料生成。
> 产物：本报告 + `matexplore/`（多智能体软件框架）+ `skill/te-screen`（taskflow 新技能）。

---

## 一、关键数字复算（结论本身没有算错）

用 `data/processed/ZT_e_all.csv` 独立重算报告中的核心相关：

| 声称的相关 | 报告值 | 复算值 | 判定 |
|---|---|---|---|
| logPF vs \|S\| (n/p) | +0.52~0.62 / — | **+0.624 / +0.515** | ✅ |
| logPF vs logσ (n/p) | −0.14~−0.22 | **−0.208 / −0.136** | ✅ |
| ZT_e vs Eg (n/p) | +0.63 / +0.76 | **+0.633 / +0.758** | ✅ |
| ZT_e vs m\* (n/p) | +0.78 | **+0.779 / +0.775** | ✅ |
| ZT_e>2 材料数 (n/p) | 131/806, 378/803 | **131/806, 378/803** | ✅ |

**结论：报告的统计数字全部准确，没有算术错误。** 问题出在"从数字到结论"的推理链条上（见下）。

---

## 二、发现的逻辑问题（重点）

### L1（最重要）："高 ZT_e = 高 ZT 候选" 隐含 κ_L 可比假设，且换算非单调
- 事实：`ZT_e = S²/L` 是 **κ_L→0 的电子上限**，真实 ZT = ZT_e × κe/(κe+κL)。
- 复算发现：**ZT_e 与 logσ 强负相关（n −0.776 / p −0.794）**，即高 ZT_e 材料电导率很低；
  而 m\* 与 logσ 更负（−0.873 / −0.901）。低 σ ⇒ 低 κe=LσT。
- 后果：`κe/(κe+κL)` 这个把"上限"折成"真实 ZT"的系数，对高 ZT_e 材料**会很小**，
  因此"排名第一的 ZT_e 材料"未必是"排名第一的真实 ZT 材料"。报告的 top 榜（AsKO2、Cl2V、FOV…）
  在没有 κ_L 时只能叫"电子上限榜"，不能叫"热电材料榜"。
- 但注意反方向也有据：复算得 **ZT_e vs logPF = +0.62 / +0.52（正）**，
  且 top10% ZT_e 的 PF 中位 ≈623（n）/467（p），bottom10% 仅 6/7。说明在 κe≪κL 极限下
  真实 ZT ≈ PF·T/κ_L，而高 ZT_e 材料的 PF 确实更高——所以 ZT_e 排名**方向大致可用**，前提是 κ_L 不比 PF 涨得更快。
- **一句话**：排名方向对，但绝对名次依赖 κ_L，这是当前最大的未闭合缺口。

### L2："宽带隙 + 重有效质量 → 高 ZT" 是 Pisarenko 关系的同义反复，且对 PF 不完全成立
- `ZT_e=S²/L` 只由 S 决定；而 S ∝ m\*·Eg 是半导体物理常识（Pisarenko/Mott 公式）。
  所以"ZT_e 与 Eg(+0.63/+0.76)、m\*(+0.78) 正相关"本质上在**重述 S 的定义式**，不是新发现。
- 真正的非平凡结论应是：**S–σ 权衡下 PF=S²σ 仍随 S 单调上升**（复算 ZT_e↔PF +0.62），
  但上升变缓（PF vs Eg 只有 +0.45/+0.49，明显弱于 ZT_e vs Eg 的 +0.63/+0.76）。
- 因此"重质量+大带隙越好"的表述**过度**了：真实 ZT 需要的是"在保住 σ 的前提下拉高 S"，
  即**中等 Eg（约 1–2 eV）、中等 m\*（约 0.5–1.5 m_e）**的甜点区，而不是"越重越宽越好"的角落。

### L3：固定 T=600K、n=1e20、常数弛豫时间 → 排名对"最优工作条件"不稳健
- 报告自己给出了 **SnSe 反例**（实验 ZT 2.6 > JARVIS ZT_e 1.51，物理上不可能），
  并归因于温度(923K vs 600K)与掺杂。这个反例的严重性被低估了：
  它说明"ZT_e 天花板"只在**同 T、同 n、同维度**下才是严格上界，跨条件外推即失效。
- 因此整张"高 ZT_e 榜"是**单点(600K, 1e20, 单层, CRTA)**的快照，不是材料的内禀排序。

### L4："p 型整体更强(378 vs 131)" 是固定 n=1e20 下的结论，不是普适的 p>n
- 378 vs 131 只说明"在 1e20 载流子浓度下 p 型更容易出高 S"，换一个掺杂浓度结论可能反转。
- 建议表述为"在 JARVIS 固定条件下 p 型高 ZT_e 占比更高"，不要写成材料普适规律。

### L5："结构↛输运"负结果受 SOAP(dummy species) 限制，需区分"几何相似"与"元素身份"
- 报告自认(C2) SOAP 把所有原子映射成 dummy species X，丢弃了元素身份。
- 我的补充：**组成(Magpie)特征与输运的相关并不为零**——电负性均值 vs ZT_e = +0.34/+0.39、
  电离能均值 +0.31/+0.35、面内面积 +0.31/+0.28、真空/√面积 −0.31/−0.28（见第三节折线图）。
  所以准确表述是"**纯几何结构相似性**不能预测输运"，而"**化学组成**"是有可用的弱-中等信号的。

### L6：WF 定律 ZT_e=S²/L 对非简并半导体只是近似
- Lorenz 数 L=2.44e-8 是金属/强简并极限值；对非简并半导体 L 偏小，故 ZT_e=S²/L 会**低估**真实电子上限。
- 影响方向：半导体候选的真实电子 ZT 上限可能比报告值略高；不影响定性结论，但应在论文标注近似。

### L7：E_hull 全 0.0 → 稳定性完全未验证，top 候选可能是亚稳/不可合成的
- 快照 `_jarvis_ehull` 全 0（报告 D4 已记）。这意味着 **AsKO2、Cl2V、FOV、ClNS 等 top 材料**
  没有任何"能量在凸包之上"的判据，可能是动力学/热力学不稳相。
- 对"预测新材料"这件事，稳定性是第一道闸：本框架在验证层把 **MACE 弛豫(opt-mace-gpu) + 声子稳定性**
  放在最前，正是为了补这个缺口。

### L8："重有效质量"是相对措辞，绝对数值并不重
- 复算 median m\* = electron 0.56 m_e / hole 1.14 m_e，其实都不算"重"（重质量通常指 >2–3 m_e）。
- 报告用"重质量角落"指"相对金属/轻质量区更重的一端"，属相对表述，对外写作建议改"更高质量端"。

---

## 三、新增：易算特征 ↔ 热电参数 的关联假设（二维折线相关图）

这正是用户要求的"把热电优质参数与结构/电子/易算特征关联起来"。对 1103 个二维材料，
计算 19 个特征与 4 个目标（ZT_e / logPF / |S| / logσ，n/p 分开）的 Spearman 相关：

**图**：`matexplore/reports/feature_target_correlation_polyline.png`（横轴=特征、纵轴=Spearman rho、
蓝实线=n 型、红虚线=p 型，4 面板=4 个目标）。

核心读图结论（全由真实数据得出）：
1. **ZT_e 与 |S| 完全同构**（ZT_e=S²/L），两者相关面板一模一样，最强的可算特征是 m\*(+0.78) 与 Eg(+0.63/+0.76)。
2. **logσ 与 ZT_e 几乎镜像反相关**：m\*(−0.87/−0.90)、Eg(−0.58/−0.66)、面内面积(−0.53/−0.55)、真空/√面积(+0.53/+0.54)。
3. **logPF 的相关最"平"**：只有 Eg(+0.45/+0.49) 和 m\*(+0.33/+0.28) 超过 0.3——PF 是 S 与 σ 的乘积，
   两股相反趋势相互抵消，这正是 S–σ 权衡的直接证据。
4. **纯组成/几何的"易算特征"也有可用信号**（无需 DFT）：电负性均值(+0.34/+0.39)、电离能均值(+0.31/+0.35)、
   面内面积(+0.31/+0.28)、真空/√面积(−0.31/−0.28)、n_sites(+0.26/+0.23)。
5. 训练 Ridge 替代模型验证"易算特征能筛多准"：14 个无-DFT 特征 → ZT_e 的 5 折 CV Spearman = **+0.35(n)/+0.49(p)**；
   → logPF = +0.13(n)/+0.24(p)。结论：易算特征可做**第一轮粗筛**（召回 top 10–20%），但精确排名必须回 Eg/m\*（即 DFT）。

**修正后的假设（比原"重质量+宽带隙"更准确）**：
> 热电优值由 **S 主导（ZT_e=S²/L）**；S 由 **Eg 与 m\*** 拉高，但 σ 被 m\* 压低。
> 因此真实最优在 **S–σ 权衡的甜点区（中等 Eg≈1–2 eV、中等 m\*）**，而非极端角落。
> **易算特征（电负性、电离能、原子半径、面内面积、层厚、原子数）对 ZT_e 有 ±0.2~0.39 的弱-中等预测力**，
> 可构成无需 DFT 的第一层筛选器。

---

## 四、新材料预测（框架已跑通一轮）

多智能体闭环 `matexplore/` 已端到端跑通（analyst→generator→validator→collector），
用"高 ZT_e 种子结构的元素族同价替换"生成候选并用易算特征替代模型打分。
Top-10 候选（完整见 `matexplore/reports/top_candidates.csv`）：

| 候选 | 预测 p-ZT_e | 预测 n-ZT_e | 预测 p-logPF | 种子 |
|---|---:|---:|---:|---|
| F4Sn | 4.35 | 1.82 | 2.77 | PbF4 |
| F4Ge | 4.28 | 1.82 | 2.70 | PbF4 |
| ClF3Pb | 4.17 | 2.04 | 2.85 | PbF4 |
| BrF3Pb | 4.13 | 1.95 | 2.84 | PbF4 |
| Br3FPb2 | 4.04 | 1.41 | 2.87 | PbBr2 |
| Br5FIn2 | 3.90 | 1.03 | 2.76 | InBr3 |
| Bi2BrFO2 | 3.83 | 0.91 | 2.87 | BiBrO |
| CdClF | 3.71 | 0.80 | 2.85 | CdCl2 |

> ⚠️ 这些是**电子上限的替代预测**（未含 κ_L，且稳定性未经 DFT 确认，见 L1/L7）。
> 正确使用方式是把它当"候选池"，按第四节的验证链逐级确认（先 MACE 稳定性，再 DFT 输运）。

---

## 五、交付物索引

- 软件框架：`matexplore/`（多智能体闭环，领域无关可复用）
- 新 taskflow 技能：`/home/wangchao/software/taskflow/skill/te-screen/`（易算特征→ZT_e/PF 快速筛选，`tf skills` 已能发现）
- 关联折线图：`matexplore/reports/feature_target_correlation_polyline.png`
- 相关面板表：`matexplore/reports/feature_target_correlation.csv`
- 候选材料：`matexplore/reports/top_candidates.csv`
- 替代模型指标：`matexplore/reports/surrogate_metrics.csv`
- 一次完整运行的产物：`matexplore/runs/<run_id>/round_000/`（假设/候选/验证/反馈 + 每个候选的 POSCAR）

