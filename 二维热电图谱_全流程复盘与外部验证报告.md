# JARVIS 二维热电输运图谱：全流程逻辑梳理 · 问题诊断 · 外部 ZT 验证 · 图与规律详解

> 一份面向"从数据到结论"的完整复盘报告。
> 被分析项目：`jarvis_2d_te_atlas`（二维热电材料多物理统一图谱，JARVIS curated `dft_2d`，1103 材料）。
> 报告作者分析范围：Phase A–F → G–K → L0 → L–O → P–S → 设计规律 → 外部数据库交叉验证。

---

## 0. 一句话总览

这项研究的**核心问题**不是"预测新材料 ZT"，而是：

> 同一种二维材料，在**结构空间、电子结构空间、热电输运空间**里的邻域关系是否一致？这些覆盖范围不同的物理视图能否通过材料身份（JID）对齐到一个公共潜在空间？

**核心方法论**是**不完备多视图流形对齐（Partial Multi-View Manifold Alignment）**：结构 backbone（1103 全覆盖）+ 若干不完整物理层（Seebeck 806、PF 802、有效质量 678…），用相同 JID 作身份锚点做联合谱嵌入。

**三个最硬的结论**：
1. **结构相似不能预测输运相似**（structure ↔ transport 距离相关 0.02–0.07 ≈ 随机）——这是最重要的稳健负结果。
2. **高 ZT 天花板 ZT_e = S²/L 由 Seebeck 主导**，需要"宽带隙 + 重有效质量"，与金属（高 σ、S≈0）在流形上清晰分离。
3. **n/p 必须分开研究**（overlap 仅 10.7%，z=60 显著但明显不同）。

---

## 1. 整体逻辑：九阶段数据流

```
JARVIS dft_2d (1103 二维材料, OptB88vdW)
        │
        ▼
[Phase A–F] 环境 + 下载 + Schema/Property Coverage Audit
        │  关键发现：κ_L=0、ZT=0（数据库本身没有），有效质量/Seebeck/PF/σ/κ_e 覆盖 60–80%
        ▼
[Phase G–K] 从 NIST 静态 XML 恢复 3 个输运本征值 + 有效质量本征值
        │  关键发现：OPTIMADE 标量 = 3 本征值算术平均（严格相等）；σ 与 κ_e 高度冗余(r≈0.96)
        ▼
[Phase L0] 主值谱物理一致性审计 → 冻结 V1 特征集
        │  关键发现：Seebeck 用 median 不用 mean；PF 配对高度敏感→只作 label；κ_e 只作 sensitivity
        ▼
[Phase L–O] 单物理视图相似图（Structure/Electronic/n-Transport/p-Transport）
        │  关键发现：structure 与 property 去相关；电子↔输运中等相关(0.32–0.38)
        ▼
[Phase P–S] JID 身份锚点 + 不完备多视图对齐（λ 扫描 + 随机锚点负对照 + 消融）
        │  关键发现：n/p 各建 Atlas（λ=0.3）；Structure 只作 linked auxiliary view
        ▼
[设计规律] ZT_e 天花板流形 + 五条设计规律 + 超晶格候选池(298 对)
        │
        ▼
[外部验证]（本次补充）dft_2d_2021 / C2DB / 2DMatPedia / Alexandria / Starrydata2
```

### 1.1 各阶段做了什么（对应脚本）

| 阶段 | 脚本 | 做什么 | 产出 |
|---|---|---|---|
| A | `00_environment.py` | 建环境、版本记录 | `reports/python_version.txt` |
| B | `01_verify_database.py` | 验证官方数据源 URL | `reports/database_source.*` |
| C | `02_download_dft2d.py` | 下载（figshare 403 → 改用 NIST OPTIMADE API） | `dft_2d_snapshot.json`（1103） |
| D | `03_validate_archive.py` | SHA256 + 完整性 | `SHA256SUMS.txt` |
| E | `04_schema_audit.py` | 78 字段逐字段 coverage | `data/audit/schema.csv` |
| F | `05_property_coverage.py` | 热电字段覆盖率矩阵 | `property_coverage.csv` |
| G | `06_recover_transport_eigenvalues.py` | NIST XML 恢复 3 本征值 | `transport_eigenvalues_raw.json` |
| H | `07_build_eigenvalues_parquet.py` | 宽表 | `transport_eigenvalues.parquet` |
| I | `08_jid_intersection_audit.py` | JID 交集审计 | `view_jid_overlap.csv` |
| J | `09_build_transport_features.py` / `10_...redundancy_audit.py` | 特征构造 + 冗余审计 | 相关矩阵、PCA |
| K | `11–15` | PF 配对、谱审计、符号分析、有效质量、表示选择 | 各 audit csv |
| L0 | `16/17` | 冻结 n/p Transport + Electronic V1 | features/…/V1.parquet |
| L–O | `18–26` | 真空轴审计、结构标准化、SOAP、6 张图、cross-view | graphs/G_*、heatmap |
| P–S | `31–36` | λ 扫描、joint alignment、随机锚点、消融、tension | manifolds/*_atlas_consensus.parquet |
| 规则 | `37–43` | Magpie、设计规律、ZT_e 天花板、超晶格对、总览图 | design_rules_overview.png |

### 1.2 贯穿全程的工程纪律（值得保留的优点）

1. **不预测/不填补缺失**：κ_L、ZT 不存在就标 `SKIPPED_NOT_AVAILABLE`，绝不插值或置 0。
2. **不自行反推物性**：`DERIVE_SIGMA=False`，禁止用 PF=S²σ 或 κ_e=LσT 反推缺失量。
3. **n/p 禁止平均**：Seebeck 一正一负，平均无物理意义，分别建模。
4. **ZT 防泄漏**：ZT/ZT_e 只作 external validation label，不进 embedding。
5. **负对照 + 稳定性扫描**：随机 JID 锚点（z=4.4/5.8 通过）、λ 扫描（稳定区间 [0.1,1.0]）、kNN k 扫描、SOAP 参数扫描。
6. **每步 checkpoint + 禁止静默吞异常**。

---

## 2. 发现的规律汇总（全部由真实数据得出）

### 规律 1：高 PF 由 |Seebeck| 主导，不是电导率
- logPF vs |S|：Spearman **+0.52~0.62**；vs logσ：**−0.14~−0.22**（负相关）。
- 高 PF(top20%) 的 |S|≈203–263 μV/K vs 低 PF 9 μV/K（21–29 倍），σ 反而更低。
- **含义**：金属（高 σ、S≈0）的 PF 极低；高 PF 必须来自高 Seebeck 的半导体。
- **独立验证**（dft_2d_2021 标量）：logPF vs |S| +0.51/+0.35，vs logσ −0.09/+0.02 → 方向一致。

### 规律 2：ZT_e = S²/L 是电子 ZT 上限（WF 定律）
- 由 ZT = S²σT/(κe+κL)，用 κe≈LσT（已验证 r≈0.96）消去 σ：**ZT_e = S²/L**，L=2.44×10⁻⁸ W·Ω/K²。
- ZT_e 与 σ 无关，只由 S 决定；真实 ZT = ZT_e × κe/(κe+κL) **恒小于** ZT_e。

### 规律 3：高 ZT_e（=高 S）需要"宽带隙 + 重有效质量"
- ZT_e vs Eg：Spearman **+0.63/+0.76**；vs m\*：**+0.78**（n/p 一致）。
- p 型整体更强：ZT_e>2 的材料 378/803（n 型仅 131/806）。

### 规律 4：S–σ 权衡（热电两难）
- S ∝ m\*·Eg（重质量+大带隙→高 S）；σ ∝ 1/m\*（轻质量→高 σ）。二者对 m\* 要求相反。
- 最优在中等带隙/质量区间（保持足够 |S| 的前提下拉高 σ）。

### 规律 5：结构不能筛热电材料（负结果，Magpie 补强后仍成立）
- 结构→输运相关 ≈ 0；结构→Eg 仅 0.11。**必须算电子结构（Eg、m\*）**，不能用结构相似度做热电筛选。

### 规律 6（输运谱物理，L0）
- 电导率谱呈 **quasi-2D 特征**：suppressed-channel contrast D_sigma 中位 ~3.1–3.3（最弱通道比次弱小 ~1000–2000 倍）；dominant-channel anisotropy A_sigma_dom 中位 ≈0（面内类近各向同性）。
- κ_e 谱与 σ 谱几乎一致（r≈0.96–0.98）→ κ_e 冗余，只作 sensitivity。
- 有效质量算术平均被面外类大值污染 300–575 倍，**median（electron 0.56 m_e / hole 1.14 m_e）才是稳健面内类有效质量**。

### 规律 7（Seebeck 符号）
- Seebeck 用 median 而非 mean（median 100% 匹配多数符号；mean 被 15% 符号异常本征值污染）。
- 符号异常强烈关联 small/zero band gap（p~1e-47）→ 是 bipolar 输运，不是噪声。

### 规律 8（n/p 分离）
- n-S vs p-S 相关 −0.40（一正一负）；n-Transport vs p-Transport overlap 10.7%（z=60）但距离相关仅 0.42 → 明显不同，分别建 Atlas。

---

## 3. 图片详细解释（图片与说明并列，不分割）

> 所有图片位于 `jarvis_2d_te_atlas/figures/`。n/p 成对图用左右并排方式紧邻展示。

### 3.1 核心结论图（Phase 设计规则）

**设计规律总览（4 面板：a 高 ZT_e 流形 / b 带隙依赖 / c S–σ 权衡 / d 超晶格候选）** — `design_rules_overview.png`

<img src="jarvis_2d_te_atlas/figures/design_rules_overview.png" width="92%" alt="设计规律总览">

- (a) p 型 joint 流形按 log10(ZT_e) 着色——高 ZT_e 集中在 Φ₁ 负端（半导体+重质量角落，⭐ 为质心）。
- (b) ZT_e vs 带隙——金属(Eg=0)全部塌在 ZT_e≈0，高 ZT_e 需要 Eg>0.5 eV。
- (c) |S| vs logσ——热电两难的对角带；高 ZT_e 在"高 |S|+中等 σ"角落。
- (d) 超晶格候选对——晶格失配 vs |S_n−S_p|，298 对，红虚线左侧=可共格。

**高 ZT_e 流形主图（n 型 | p 型 两栏）** — `high_ZT_manifold.png`

<img src="jarvis_2d_te_atlas/figures/high_ZT_manifold.png" width="92%" alt="高ZT_e流形">

- n/p 各自 joint 坐标 Φ₁–Φ₂，magma 色 = log10(ZT_e)；两型都在 Φ₁ 同一侧呈现高 ZT_e 角落。

### 3.2 输运谱物理图（L0）

**σ 谱各向异性：D_sigma vs A_sigma_dom（n | p）** — 点集中在 D 大、A_dom≈0，即"一个通道被压扁、两通道近简并"的 quasi-2D 谱。

<table><tr><td><img src="jarvis_2d_te_atlas/figures/sigma_D_vs_A_n.png" width="100%" alt="sigma D vs A n"></td><td><img src="jarvis_2d_te_atlas/figures/sigma_D_vs_A_p.png" width="100%" alt="sigma D vs A p"></td></tr></table>

**κ_e 谱各向异性：D vs A_dom（n | p）** — κ_e 的 D/A 与 σ 几乎一致（r≈0.96–0.98）→ κ_e 冗余。

<table><tr><td><img src="jarvis_2d_te_atlas/figures/kappa_D_vs_A_n.png" width="100%" alt="kappa D vs A n"></td><td><img src="jarvis_2d_te_atlas/figures/kappa_D_vs_A_p.png" width="100%" alt="kappa D vs A p"></td></tr></table>

**σ 谱维度诊断（n | p）**

<table><tr><td><img src="jarvis_2d_te_atlas/figures/sigma_dimensionality_n.png" width="100%" alt="sigma dimensionality n"></td><td><img src="jarvis_2d_te_atlas/figures/sigma_dimensionality_p.png" width="100%" alt="sigma dimensionality p"></td></tr></table>

**Seebeck 符号异常 vs 带隙（n | p）** — 异常集中在 gap=0，是 bipolar 输运证据。

<table><tr><td><img src="jarvis_2d_te_atlas/figures/seebeck_sign_vs_gap_n.png" width="100%" alt="seebeck sign vs gap n"></td><td><img src="jarvis_2d_te_atlas/figures/seebeck_sign_vs_gap_p.png" width="100%" alt="seebeck sign vs gap p"></td></tr></table>

**Seebeck mean vs median（n | p）** — mean 被 15% 符号异常本征值污染，median 稳健。

<table><tr><td><img src="jarvis_2d_te_atlas/figures/seebeck_mean_vs_median_n.png" width="100%" alt="seebeck mean vs median n"></td><td><img src="jarvis_2d_te_atlas/figures/seebeck_mean_vs_median_p.png" width="100%" alt="seebeck mean vs median p"></td></tr></table>

**PF 本征值配对歧义直方图（n | p）** — 中位歧义 0.47–0.64，PF 只能作 external label，不进 embedding。

<table><tr><td><img src="jarvis_2d_te_atlas/figures/pf_pairing_ambiguity_hist_n.png" width="100%" alt="pf pairing n"></td><td><img src="jarvis_2d_te_atlas/figures/pf_pairing_ambiguity_hist_p.png" width="100%" alt="pf pairing p"></td></tr></table>

**有效质量谱（mean vs median）** — mean 被面外类大值污染 300–575 倍，median（electron 0.56 m_e / hole 1.14 m_e）才是稳健面内类有效质量。

<img src="jarvis_2d_te_atlas/figures/effective_mass_spectrum.png" width="70%" alt="effective mass spectrum">

### 3.3 跨视图几何图（L–O，3 张热力图）

**视图 JID 交集热力图** — 各视图材料集合重叠（n-S∩σ∩κe = 806 完全一致）。

<img src="jarvis_2d_te_atlas/figures/view_jid_overlap_heatmap.png" width="70%" alt="view jid overlap">

**视图间距离相关热力图** — Structure↔property ≈0.02–0.07（冷色≈去相关），Electronic↔Transport 0.32–0.38，n↔p 0.42。

<img src="jarvis_2d_te_atlas/figures/view_distance_correlation_heatmap.png" width="70%" alt="view distance correlation">

**视图邻域重叠热力图（k=15）** — 只有 Structure-vs-Eg(z=22)、Electronic_p-vs-Transport_p(z=2.7)、n-vs-p Transport(z=60) 显著；Structure↔Transport ≈1.5% ≈随机。

<img src="jarvis_2d_te_atlas/figures/view_neighbor_overlap_heatmap.png" width="70%" alt="view neighbor overlap">

**距离相关热力图的"化学空间"版本：同一锚材料在两视图中的近邻簇** — `cross_view_neighbor_clusters_chemical_space.png`

把"两个距离的相关系数"具象化为化学空间中**两个近邻簇的相对位置**：以 MoS2（★）为锚点，画出它在视图 A 和视图 B 里各自的 k=15 近邻簇（蓝/红），X 为簇质心，虚线为质心距离。

<img src="jarvis_2d_te_atlas/figures/cross_view_neighbor_clusters_chemical_space.png" width="92%" alt="cross view neighbor clusters in chemical space">

- **左（Structure vs n-Transport）**：近邻重叠 **0%**、质心距离 4.31（1.15×全局标准差）——MoS2 的"结构近邻"与"输运近邻"是完全不同的两批材料，在化学空间里分居两处。
- **中（Electronic vs n-Transport）**：重叠 **20%**、质心距离 2.27（0.60×）——两簇明显靠近，与"电子↔输运中等相关(0.38)"一致。
- **右（n-Transport vs p-Transport）**：重叠 13%、质心距离 2.65（0.70×）——相关但不同。
- **读图结论**：化学空间里"两个簇分得越开 = 两个视图越去相关"，与热力图的 ρ_dist 数值一一对应——**这是对"结构↛输运"负结果最直观的几何证据**。

### 3.4 外部验证图（本次新增，3 张）

**热电输运版本稳定性：dft_2d vs dft_2d_2021** — Seebeck/PF 散点全落在 y=x 红线（Pearson=1.000，差异=0）。

<img src="jarvis_2d_te_atlas/figures/external_verify_te_version_stability.png" width="92%" alt="te version stability">

**带隙交叉验证：JARVIS vs 2DMatPedia**（n=906，Pearson 0.87 / Spearman 0.88）。

<img src="jarvis_2d_te_atlas/figures/external_verify_gap_2dmatpedia.png" width="55%" alt="gap 2dmatpedia">

**剥离能交叉验证：JARVIS vs 2DMatPedia**（meV→eV 换算后比值 1.006，Spearman 0.84）。

<img src="jarvis_2d_te_atlas/figures/external_verify_exfoliation.png" width="55%" alt="exfoliation">

---

## 4. 问题诊断与优化建议（重点）

### 4.1 数据层面问题

| # | 问题 | 证据 | 建议 |
|---|---|---|---|
| D1 | **无 κ_L / 真实 ZT**（硬约束） | dft_2d 的 κ_L、ZT 覆盖 0% | 用 Starrydata2 实验 ZT 做外部锚点（见 §5）；或对候选材料用 Slack/Debye-Cahill 估计 κ_L（需声明为模型） |
| D2 | **有效质量 mean 被污染 300–575 倍** | 面外类本征值 ~1e3–1e4 | 已改用 median ✓；建议在论文明确标注"OPTIMADE avg_mass 不可用" |
| D3 | **Seebeck mean 被 15% 符号异常污染** | sign violation 15% | 已改用 median ✓ |
| D4 | **E_hull 全 0.0**（round1 报告标注"全 0.0"） | 快照 `_jarvis_ehull` 全部 0 | 说明 JARVIS dft_2d 未存真实 ehull；稳定性分析需另取（C2DB ehull 或 Alexandria e_above_hull） |
| D5 | **标量字段 -99999 表示缺失** | schema 中大量 -99999 | 已在 17 脚本正确判缺；但易被误当数值，建议全流程统一 `np.nan` |

### 4.2 方法层面问题

| # | 问题 | 证据/后果 | 建议 |
|---|---|---|---|
| M1 | **3 本征值无本征向量 → 无法断言晶向** | XML 只存对角化本征值 | 全程已用 principal-value 语言 ✓；若要晶向需回原 VASP 计算 |
| M2 | **成分匹配会混入多晶型/多公式单元** | Bi2Se3 有 Bi2Se3(5原子) 与 Bi4Se6(10原子) 两个 JID；C2DB 同成分含 1T/2H | 匹配用 **GCD 归一化的组成**；论文报告重叠时注明 polymorph 混叠 |
| M3 | **PF=S²σ/1e6 是派生量且配对敏感** | PF 本征值配对歧义 0.47–0.64 | 已正确降级为 external label ✓ |
| M4 | **Structure 用 SOAP(dummy species) + 元素比例，融合权重 0.5/0.5 是人为设定** | 邻域对融合权重中等敏感(0.25/0.5=0.80) | 已做敏感性 ✓；可再对比 composition-only / geometry-only 两分支 |
| M5 | **k=15 统一，但 Electronic 图天然分裂金属/半导体两个分量** | giant=0.64–0.70 | 已在 joint 阶段正确处理；建议报告明确"二分非缺陷，是物理" |

### 4.3 结论层面问题（需谨慎表述）

| # | 问题 | 说明 | 建议 |
|---|---|---|---|
| C1 | **"高 ZT 流形"是 ZT_e（电子上限），不是真实 ZT** | 报告已诚实标注 | 对外必须写"ZT_e 天花板"；真实 ZT 需 κ_L |
| C2 | **结构↛输运 的负结果可能部分源于 SOAP(dummy species) 丢弃了元素身份** | 但 L-O 已测 geometry-only 与 composition 都去相关，且 Magpie(37) 补强后仍成立 | 结论稳健；建议再补 species-sensitive SOAP（当前因 81 元素维度爆炸 SKIPPED）作最终确认 |
| C3 | **ZT_e 在 T=600K 固定、n=1e20 固定** | 与实验"峰值温度/最优掺杂"不可直接比 | §5 的 SnSe 反例正源于此，见下 |
| C4 | **负结果"结构相似但输运不同"的候选对未做 κ_L 验证** | 超晶格 298 对无 κ_L | 声明为候选池，未升级 |

### 4.4 优化优先级建议
1. **最高**：引入 κ_L 来源（Starrydata2 实验 κ_L 曲线有 6740 条 Lattice thermal conductivity，可用于候选材料的 κ_L 量级校验）。
2. **高**：把 ZT_e 折成真实 ZT 的**下界**（用实验 κ_L 或 Debye-Cahill 估计 κ_L 上界 → ZT 下界）。
3. **中**：species-sensitive SOAP（先降维再算）以最终确认结构↛输运。
4. **中**：对 top 候选（WSe2、CrBr2、CaClF 等）做独立 BoltzTraP/AMSET 复算（需新计算，作为 follow-up）。

---

## 5. 带真实 ZT 的数据库：Starrydata2（本次新找到）

### 5.1 发现与下载
- 上一轮报告（design_rules_final.md §四）记录"κL/ZT 数据库全部下载失败"。本次找到并成功下载：
- **Starrydata2**（日本 NIMS 的实验热电数据库，含真实测量 ZT）：
  - 最新数据（GitHub Releases）：`ThermoelectricMaterials_curves.csv.gz`（156,206 条曲线）、`_samples.csv.gz`、`_papers.csv.gz`。
  - 稳定下载地址：`https://github.com/starrydata/starrydata_datasets/releases/latest/download/<文件>`
  - 数据文件存于 `data/raw/external/starrydata2/`，SHA256 已入 `SHA256SUMS.txt`。

### 5.2 Starrydata2 里的 ZT 数据
- **ZT 曲线 20,729 条**，覆盖 **13,449 个成分**、20,347 个样品；另有 Seebeck 37,441 条、热导率 23,486 条、**晶格热导率 6,740 条**、功率因子 17,407 条。
- 提取峰值 ZT 后（`starrydata_zt_extracted.csv`）：中位 ZT≈0.52，p90≈1.23，ZT>2 有 ~251 条。

### 5.3 ⚠️ 数据质量陷阱（务必处理）
Starrydata2 存在 **OCR/单位缩放错误**，例如原始峰值出现 `Bi0.5Sb1.5Te3 ZT=13054`、`Bi2Te2.8Se0.2 ZT=9405`（应为 ~1 量级）、`Ti0.9Sc0.1CoSb ZT=245`。因此必须：
1. 物理过滤 `0 < ZT < 5`；
2. 温度过滤 `100 < T_max < 1500 K`（原始有 T_max≈1e24 K 的脏数据）；
3. 即使过滤后，Bi2Te3 家族仍偏高（~4.3–4.7 vs 文献 ~1.1），SnSe(~3.1)、PbTe(~2.8)、GeTe-TAGS(~2.9)、Cu2Se(~2.6) 与文献记录吻合。

### 5.4 关键验证：JARVIS ZT_e 天花板 vs 实验 ZT（`external_verify_zte_vs_experimental.csv`）

| 材料 | JARVIS 2D 单层 ZT_e（S²/L，600K）| 实验 ZT（bulk，文献/Starrydata2）| 关系 |
|---|---:|---:|---|
| Bi2Te3 | p 2.69 / n 0.40 | ~1.1 | 天花板 > 实验 ✓ |
| Bi2Se3 | p 2.59 / n 0.30 | 0.35 | ✓ |
| MoS2 | p 2.59 / n 0.89 | ~0.1 | ✓ |
| WS2 | n 5.36 / p 2.09 | ~0.15 | ✓ |
| WSe2 | n 6.29 / p 0.66 | ~0.1 | ✓ |
| MoSe2 | p 1.81 / n 0.91 | 0.05 | ✓ |
| SnSe2 | p 2.68 / n 1.02 | ~0.1 | ✓ |
| InSe | n 1.34 / p 1.24 | ~0.1 | ✓ |
| GaSe | p 3.76 | 0.05 | ✓ |
| **SnSe** | **n 1.51 / p 0.46** | **2.6** | **⚠️ 反例** |

**读表结论**：
- 对绝大多数 2D 半导体，JARVIS 单层的**电子 ZT 上限远高于实验 bulk ZT**（如 MoS2/WSe2 高出 1–2 个数量级），符合"ZT_e 是 κ_L→0 的上界 + 2D 量子限域增强 Seebeck"的双重解释，**验证了 ZT_e 天花板方法的方向正确**。
- **SnSe 是重要反例**：实验 ZT 2.6 > JARVIS ZT_e 1.51。原因：① 实验 SnSe 峰值在 **923 K**，而 JARVIS 固定 600 K（S 随 T 上升，600K 的 ZT_e 被低估）；② JARVIS 是**单层**、固定 n=1e20、常数弛豫时间，与 bulk 最优掺杂不可直接比较。**教训：ZT_e 天花板只在与实验同温度、同掺杂比较时才构成严格上界**，不能跨条件外推。

### 5.5 下一步建议（基于 ZT 数据）
1. 用 Starrydata2 的**晶格热导率 κ_L 曲线（6740 条）**给 JARVIS 候选材料的 κ_L 量级，把 ZT_e 折成**真实 ZT 下界**。
2. 对 JARVIS 高 ZT_e 材料（WS2、WSe2、CrBr2、CaClF 等）查 Starrydata2 有无实验对应，做逐材料核对。
3. 清洗 Starrydata2 的 ZT 脏数据（单位缩放、温度异常）后，可作为 ML 模型的 external ZT label（不进入流形，只作验证）。

---

## 6. 总结

**这套分析的方法学是扎实的**：数据审计→本征值恢复→物理审计→特征冻结→单视图图→多视图对齐→随机负对照→设计规律→外部验证，每一步都有 checkpoint 和物理约束（不补缺失、n/p 分离、ZT 防泄漏、负对照）。

**三个稳健结论**（结构↛输运、ZT_e=S²/L 由 S 主导、n/p 分离）经得起外部数据检验：
- 热电输运值在 dft_2d 与 dft_2d_2021 间**逐条一致**（Pearson=1.0）；
- 带隙/晶格/剥离能与 C2DB、2DMatPedia **一致**（带隙 Spearman 0.72–0.88，晶格 <0.4%，剥离能比值 1.006）；
- ZT_e 天花板与实验 ZT 的**方向关系成立**（绝大多数材料 ceiling > 实验）。

**最大的缺口仍是 κ_L 与真实 ZT**——本次已打通 Starrydata2（实验 ZT + κ_L 曲线），补齐这个缺口是"把 ZT_e 天花板升级为真实 ZT 排名"的直接路径，也是这份工作从"负结果 + 规则"走向"可用热电筛选工具"的关键一步。

---

## 附录：本次新增产物清单
- 报告：`jarvis_2d_te_atlas/reports/external_database_verification.md`、`reports/external_download_metadata.json`
- 审计表：`data/audit/external_verify_dft2d_vs_2021.csv`、`external_verify_jarvis_vs_c2db.csv`、`external_verify_jarvis_vs_2dmatpedia.csv`、`external_verify_gap_alexandria.csv`、`external_verify_zte_vs_experimental.csv`
- ZT 数据：`data/raw/external/starrydata2/`（curves/samples/papers + `starrydata_zt_extracted.csv`、`starrydata_zt_sane.csv`）
- 图：`figures/external_verify_te_version_stability.png`、`external_verify_gap_2dmatpedia.png`、`external_verify_exfoliation.png`
