# Joint Manifold Feasibility — n-type

结论：**SUPPORTED（moderate）**

- 最优配置：n-Property（Eg + m_electron + n_transport），λ=0.3。
- P_transport=0.607，P_Eg=0.554，P_mass=0.622（moderate preservation）。
- 随机 anchor 对照 z=4.4（通过）。
- Structure 不进公共空间（P_structure 0.13–0.51）。

限制：P_v 未达 0.70 理想值（~0.55–0.64），且 P_Eg 受金属 clique 影响偏低。因此为 **moderate** 而非 strong support。
