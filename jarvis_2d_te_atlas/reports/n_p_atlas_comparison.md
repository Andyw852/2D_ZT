# n/p Atlas 比较（S）

对 802 个共同 n/p JID，比较两张 property 图谱的 consensus 坐标（λ=0.3）：

- consensus pairwise distance Spearman = **0.766**。
- consensus kNN(15) overlap = **0.495**。

结论：n-Transport 与 p-Transport 的公共几何**相关但明显不同**（overlap 约 50%，距离相关 0.77），支持分别建立 n/p 两张 Atlas，不合并。
